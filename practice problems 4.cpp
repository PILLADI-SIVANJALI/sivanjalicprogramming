#include<stdio.h>
#include<math.h>
main(){
	int a, b, sum_result;
	printf("enter the numbers");
	scanf("%d%d",&a,&b);
	sum_result=(a,b);
	printf("the sum of %d and %d is %d\n",a,b,sum_result);
}
